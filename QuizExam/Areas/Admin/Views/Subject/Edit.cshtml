@using QuizExam.Core.Models.Subject
@model NewSubjectVM

@{
    ViewData["Title"] = "Редакция";
}

<div class="row d-flex justify-content-center">
    <div class="col-4">
        <div class="card card-info">
            <div class="card-header">
                <h3 class="card-title">Редакция на учебен предмет</h3>
            </div>
            <form id="edit" method="post">
                <div class="card-body">
                    <input type="hidden" name="Id" value="@Model.Id"/>
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="Name" class="form-label"></label>
                        <input asp-for="Name" type="text" class="form-control" aria-required="true">
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>
                <div class="card-footer">
                    <button type="submit" class="btn btn-info">Редактирай</button>
                    <a asp-action="GetSubjectsList" class="btn btn-outline-secondary" style="width:auto">Назад</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
