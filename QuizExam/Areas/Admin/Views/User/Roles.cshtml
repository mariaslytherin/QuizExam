@using QuizExam.Core.Models
@model List<UserRolesVM>

@{
    ViewData["Title"] = "Редакция на роли";
}

<div class="row d-flex justify-content-center">
    <div class="col-4">
        <div class="card card-info">
            <div class="card-header">
                <h3 class="card-title">Редакция на роли</h3>
            </div>
            <form id="roles" method="post">
                <div class="card-body">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label value="@ViewBag.UserFullName" class="form-label"></label>
                        <input value="@ViewBag.UserFullName" class="form-control" readonly>
                    </div>
                    <div class="form-group">
                        <label>Роли:</label>
                        @for (int i = 0; i < Model.Count; i++)
                        {
                            <div class="form-check m-1">
                                <input type="hidden" asp-for="@Model[i].RoleId" />
                                <input type="hidden" asp-for="@Model[i].RoleName" />
                                <input asp-for="@Model[i].Selected" class="form-check-input" />
                                <label class="form-check-label" asp-for="@Model[i].Selected">
                                    @Model[i].DisplayName
                                </label>
                            </div>
                        }
                    </div>
                </div>
                <div class="card-footer">
                    <button type="submit" asp-area="Admin" asp-action="SetRoles" asp-controller="User" asp-route-id="@ViewBag.UserId" class="btn btn-info">Запази</button>
                    <a asp-action="GetAllUsers" class="btn btn-outline-secondary" style="width:auto">Назад</a>
                </div>
            </form>
        </div>
    </div>
</div>
