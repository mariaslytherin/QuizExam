@using QuizExam.Core.Models.Exam
@model IEnumerable<ExamListVM>
@{
}

<div class="row">
    <div class="col-12">
        <a asp-action="New" class="btn btn-block btn-outline-success" style="width: 130px; margin-bottom: 10px;">New Exam</a>
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Exams</h3>
                @*<div class="card-tools">
                    <div class="input-group input-group-sm" style="width: 150px;">
                    <input type="text" name="table_search" class="form-control float-right" placeholder="Search">
                    <div class="input-group-append">
                    <button type="submit" class="btn btn-default">
                    <i class="fas fa-search"></i>
                    </button>
                    </div>
                    </div>
                    </div>*@
            </div>

            <div class="card-body table-responsive p-0" style="overflow: inherit !important">
                <table class="table table-hover text-nowrap">
                    <thead>
                        <tr>
                            <th style="text-align: center;">Title</th>
                            <th style="text-align: center;">Subject</th>
                            <th style="text-align: center;">Create Date</th>
                            <th style="text-align: center;">Published</th>
                            <th style="text-align: center;">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var exam in Model)
                        {
                            <tr @(exam.IsActive == "No" ? ("style=" + "background-color:" + "gray;") : string.Empty)>
                                <td style="text-align: center;">@exam.Title</td>
                                <td style="text-align: center;">@exam.SubjectName</td>
                                <td style="text-align: center;">@exam.CreateDate</td>
                                <td style="text-align: center;">@exam.IsActive</td>
                                <td style="text-align: center;">
                                    <div class="btn-group">
                                        <button type="button" class="btn btn-info">Actions</button>
                                        <button type="button" class="btn btn-info dropdown-toggle dropdown-icon" data-toggle="dropdown" aria-expanded="false">
                                        </button>
                                        <div class="dropdown-menu" role="menu" style="">
                                            <a class="dropdown-item" asp-action="edit" asp-controller="Exam" asp-route-id="@exam.Id">Edit</a>
                                            <form method="post">
                                                <button type="submit" class="dropdown-item" asp-action="activate" asp-controller="Exam" asp-route-id="@exam.Id">Activate</button>
                                                <button type="submit" class="dropdown-item" asp-action="deactivate" asp-controller="Exam" asp-route-id="@exam.Id">Deactivate</button>
                                                <button type="submit" class="dropdown-item" asp-action="delete" asp-controller="Exam" asp-route-id="@exam.Id">Delete</button>
                                            </form>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
